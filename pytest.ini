[pytest]
addopts = -rsxX -l --tb=short --strict-markers
xfail_strict = true

markers =
	unit: marks tests as unit tests

testpaths = tests/unit

pythonpath = 
	. 
	layers

log_cli = True
log_cli_level = INFO